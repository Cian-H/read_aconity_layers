version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    pre_install:
      - apt install -y cargo

sphinx:
   configuration: docs/conf.py

python:
   install:
   - requirements: docs/requirements.txt
